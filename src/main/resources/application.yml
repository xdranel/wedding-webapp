spring:
  application:
    name: myweddinginvitation-webapp
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/wedding}
    username: ${DB_USERNAME:wedding}
    password: ${DB_PASSWORD:wedding}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 21MB
      max-request-size: 21MB
  config:
    import: optional:file:.env[.properties]
app:
  bootstrap-admin:
    username: ${ADMIN_USERNAME}
    password: ${ADMIN_PASSWORD}
  media-directory: ${MEDIA_DIRECTORY:./data/media}
  invitation:
    base-url: ${INVITATION_BASE_URL:http://localhost:8080/i}
    signing-secret: ${INVITATION_SIGNING_SECRET}
server:
  forward-headers-strategy: framework
management:
  endpoints:
    web:
      exposure:
        include: health
